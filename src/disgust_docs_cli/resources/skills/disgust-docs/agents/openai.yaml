interface:
  display_name: "Disgust Docs"
  short_description: "Work with shared Markdown docs via CLI"
  default_prompt: "Use $disgust-docs to sync, inspect, and safely update shared Markdown docs through the disgust-docs CLI."
